<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="text-white text-md bg-red-600 p-2 rounded-md"> <-- LOGOUT </a>
        </div>
        <h5 class="mb-5 text-xl">Edit your post here</h6>
       <form action="/update/<%= post._id %>" method="post">
        <textarea name="content" placeholder="write your content"  class="p-3 block w-1/3 resize-none bg-transparent rounded-md border-2 border-zinc-800"><%= post.content %></textarea>
        <input type="submit" value="Update post" class="px-3 py-2 mt-3 bg-yellow-500 text-black block rounded-md  w-40">
       </form>
    </div>
</body>
</html>