<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="text-white text-md bg-red-600 p-2 rounded-md"> <-- LOGOUT </a>
        </div>
        <div class="flex gap-3">
            <div class="w-10 h-10 rounded-lg">
                <img class="w-full h-full object-cover overflow-hidden" src="/images/uploads/<%= user.profile %>" alt="User profile image">
            </div>
            
            <h3 class="text-2xl m-3">hello,<%= user.username %></h3>
        </div>
        <h5 class="mb-5">you can create your post here</h6>
       <form action="/post" method="post">
        <textarea name="content" placeholder="write your content"  class="p-3 block w-1/3 resize-none bg-transparent rounded-md border-2 border-zinc-800"></textarea>
        <input type="submit" value="create new post" class="px-3 py-2 bg-blue-600 block rounded-md  w-40">
       </form>
       <div class="posts mt-16">
        <h3 class="text-zinc-700">your posts</h3>
        <div class="postcontainer mt-5 flex flex-wrap gap-1">
            <%user.posts.reverse().forEach(function(post){ %>
                <div class="post rounded-md  w-1/3 p-4 bg-zinc-800 border-2 border-zinc-700">
                    <h4 class="text-blue-600">@<%= user.username %></h4>
                    <br>
                    <p class="text-sm tracking-tight"><%= post.content %></p>
                    <div class="btns flex justify-between">
                        <div class="flex  mt-5 justify-center items-center">
                            <h6 class="mr-2"><%= post.likes.length %></h6>
                            <a href="/like/<%= post._id %>" class=" text-blue-600">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like": "Unlike" %>
                            </a>
                        </div>
                        
                        <a href="/edit/<%= post._id %>" class="mt-5 text-zinc-500">edit</a>
                    </div>
                </div>
           <% }) %>

        </div>
       </div>
    </div>
</body>
</html>